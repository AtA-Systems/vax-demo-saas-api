{
  "name": "vax-demo-saas-api",
  "version": "0.1.0",
  "private": true,
  "description": "Demo SaaS API used to showcase the VAX evidence scan workflow.",
  "type": "module",
  "scripts": {
    "dev": "node --watch src/server.js",
    "start": "node src/server.js",
    "test": "node --test"
  },
  "engines": {
    "node": ">=20"
  },
  "dependencies": {
    "express": "^4.21.2"
  }
}
